import { ReactNode } from "react";

export const metadata = {
  title: "Resume | Abdul Wasay Minhas",
};

export default function ResumeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
